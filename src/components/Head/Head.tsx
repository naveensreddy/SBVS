import React from 'react';
import styled from 'styled-components';
import Helmet from'react-helmet';

const Head =()=>{
    return(
      <Helmet>
        <title>SVBC</title>
      </Helmet>
    )
}

export default Head;